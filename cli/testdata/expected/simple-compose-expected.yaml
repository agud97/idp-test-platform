apiVersion: idp.platform.io/v1alpha1
kind: Environment
metadata:
  name: converted
  namespace: env-fixture-team-converted
spec:
  owner: fixture-user
  team: fixture-team
  components:
    - name: frontend
      type: webapp
      enabled: true
      imageTag: "1.27"
      replicas: 1
      configOverrides:
        APP_ENV: "test"
        LOG_LEVEL: "debug"
