backstage:
  appConfig:
    integrations:
      github:
        - host: github.com
          token: ${GITHUB_TOKEN}
    catalog:
      locations:
        - type: file
          target: /app/catalog/all-components.yaml
          rules:
            - allow:
                - Component
                - System
                - Group
                - User
