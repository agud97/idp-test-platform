apiVersion: idp.platform.io/v1alpha1
kind: Environment
metadata:
  name: reconciliation-smoke
spec:
  owner: group:default/platform
  team: test
  components:
    - name: web
      type: webapp
      enabled: true
